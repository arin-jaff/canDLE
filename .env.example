# Gemini API (for puzzle generation)
GEMINI_API_KEY=

# Google OAuth (frontend — prefix with VITE_ so Vite exposes it)
VITE_GOOGLE_CLIENT_ID=

# Google AdSense (frontend — optional)
VITE_ADSENSE_CLIENT=

# The following are set in Cloudflare Pages dashboard (Settings → Environment Variables):
# GOOGLE_CLIENT_ID=        (same value as VITE_GOOGLE_CLIENT_ID, used server-side)
# JWT_SECRET=              (random 32+ char string for signing session JWTs)
# GEMINI_API_KEY=          (same value as above, used server-side for admin regen)
# GITHUB_TOKEN=            (GitHub fine-grained PAT with Contents read/write on this repo)
# GITHUB_REPO=             (e.g. "arin-jaff/canDLE")
#
# D1 database (DB binding) is configured in Cloudflare dashboard:
#   Pages → Settings → Functions → D1 database bindings → Variable name: DB
